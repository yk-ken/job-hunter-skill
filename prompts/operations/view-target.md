当用户运行 `/job-hunter target` 时，读取 `data/job-target.csv`。

如果文件不存在或仅有表头行：

```
目标列表为空，暂无标记为目标的岗位。
```

如果有记录，以表格形式展示：

```
目标岗位（共 {count} 个）：

| # | 岗位名称 | 公司 | 薪资 | 备注 | 分类日期 |
|---|---------|------|------|------|---------|
| 1 | xxx     | xxx  | xxx  | xxx  | xxx     |
```
